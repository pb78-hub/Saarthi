import { HealthField } from "@/types/grievance";
const labels: Record<HealthField, string> = { issueDescribed: "What happened", location: "Where it happened", department: "Public service identified", dateOrTimeframe: "When it started", desiredOutcome: "What you need", supportingDetail: "Useful context added" };
export function HealthScoreSeal({ captured }: { captured: HealthField[] }) { const percentage = Math.round(captured.length / 6 * 100); const color = percentage === 100 ? "var(--ready)" : percentage >= 50 ? "var(--pending)" : "var(--blue)"; return <><div className="seal" style={{ "--seal-color": color } as React.CSSProperties}><svg viewBox="0 0 100 100" aria-label={`${percentage}% of details captured`}><circle className="base" cx="50" cy="50" r="43"/><circle className="progress" cx="50" cy="50" r="43" pathLength="100" strokeDasharray="100" strokeDashoffset={100 - percentage}/></svg><div className="seal-copy"><span className="seal-number">{percentage}</span><span className="seal-label">{percentage === 100 ? "ready" : "captured"}</span></div></div><ul className="checklist">{(Object.keys(labels) as HealthField[]).map(field => <li className={captured.includes(field) ? "done" : ""} key={field}><span className="dot"/><span>{labels[field]}</span></li>)}</ul></>; }
